using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GetLastRoadSpawned
{
    //private static Vector3 lastRoadSpawnedPos;
    //public static Vector3 LastRoadSpawnedPos { get { return LastRoadSpawnedPos; }
    //    set { LastRoadSpawnedPos = value; } }

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
